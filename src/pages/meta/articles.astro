---
import ArticleTable from '@layouts/ArticleTable.astro';
import { latestMetaArticles } from '../../utils/helpers';

let allArticles = await latestMetaArticles();
---

<ArticleTable allArticles={allArticles} title="Meta"/>